---
phase: 09-docs-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - BILLING-01
  - BILLING-02
  - BILLING-03
  - BILLING-04
  - BRAND-01
  - BRAND-02
  - RESULTS-02

must_haves:
  truths:
    - "npm run build in apps/web exits 0 — confirming RESULTS-02 PDF export code compiles"
    - "All 7 target requirements show [x] in REQUIREMENTS.md"
    - "All 26 v1 requirements show Complete in the traceability table"
    - "ROADMAP.md progress table shows Complete for phases 4 through 8"
    - "ROADMAP.md phase detail sections have correct plan checkbox counts"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes and traceability table"
      contains: "[x] **BILLING-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Updated progress table and plan checkboxes"
      contains: "Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Both reflect same completion state for phases 5 and 6"
      pattern: "Complete"
---

<objective>
Close the documentation drift gap by updating REQUIREMENTS.md checkboxes and ROADMAP.md progress table to accurately reflect all completed work across phases 4-8.

Purpose: Seven requirements (BILLING-01..04, BRAND-01, BRAND-02, RESULTS-02) were implemented in Phases 5 and 6 but documentation still shows them as incomplete. This creates confusion for future planning and milestone audits.

Output: Updated REQUIREMENTS.md and ROADMAP.md with all 26 v1 requirements showing Complete.
</objective>

<execution_context>
@C:/Users/josha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/josha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-docs-sync/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify apps/web build passes (RESULTS-02 prerequisite per D-01)</name>
  <files>apps/web/node_modules (install target, not modified in repo)</files>
  <read_first>
    - apps/web/package.json
    - .planning/phases/06-brand-ui-and-export/VERIFICATION.md
  </read_first>
  <action>
Per D-01, Phase 6 VERIFICATION.md flagged that @react-pdf/renderer@4.4.1 is listed in package.json but was never installed (node_modules missing).

1. Run `cd apps/web && npm install` to install all dependencies including @react-pdf/renderer
2. Run `cd apps/web && npm run build` and confirm exit code 0
3. This confirms RESULTS-02 (PDF export via @react-pdf/renderer) compiles successfully

Do NOT mark RESULTS-02 as complete until build exits 0. If build fails, diagnose and fix the build error before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd apps/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npm run build` exits with code 0
    - `apps/web/node_modules/@react-pdf/renderer/package.json` exists (package installed)
  </acceptance_criteria>
  <done>apps/web builds successfully with @react-pdf/renderer installed, confirming RESULTS-02 code compiles</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md to reflect completed work (D-02 through D-07)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/05-freemium-and-billing/05-VERIFICATION.md
    - .planning/phases/06-brand-ui-and-export/VERIFICATION.md
    - .planning/phases/07-is-ranged-schema-fix/07-VALIDATION.md
    - .planning/phases/08-phase04-verification/08-VERIFICATION.md
  </read_first>
  <action>
**REQUIREMENTS.md changes (per D-02, D-03, D-04):**

1. Update these 7 checkbox lines from `- [ ]` to `- [x]`:
   - Line with `**BILLING-01**` — change `- [ ]` to `- [x]`
   - Line with `**BILLING-02**` — change `- [ ]` to `- [x]`
   - Line with `**BILLING-03**` — change `- [ ]` to `- [x]`
   - Line with `**BILLING-04**` — change `- [ ]` to `- [x]`
   - Line with `**BRAND-01**` — change `- [ ]` to `- [x]`
   - Line with `**BRAND-02**` — change `- [ ]` to `- [x]`
   - Line with `**RESULTS-02**` — change `- [ ]` to `- [x]`

2. Update the traceability table for these 7 rows (per D-03):
   - `BILLING-01` row: Phase column = `Phase 5 — Freemium and Billing`, Status = `Complete`
   - `BILLING-02` row: Phase column = `Phase 5 — Freemium and Billing`, Status = `Complete`
   - `BILLING-03` row: Phase column = `Phase 5 — Freemium and Billing`, Status = `Complete`
   - `BILLING-04` row: Phase column = `Phase 5 — Freemium and Billing`, Status = `Complete`
   - `BRAND-01` row: Phase column = `Phase 6 — Brand, UI and Export`, Status = `Complete`
   - `BRAND-02` row: Phase column = `Phase 6 — Brand, UI and Export`, Status = `Complete`
   - `RESULTS-02` row: Phase column = `Phase 6 — Brand, UI and Export`, Status = `Complete`

3. Verify all 26 rows in the traceability table show `Complete` (per D-04).

**ROADMAP.md changes (per D-05, D-06, D-07):**

4. Update the Progress table (around line 230+):
   - Phase 5 row: `| 5. Freemium and Billing | 3/3 | Complete | 2026-04-12 |`
   - Phase 6 row: `| 6. Brand, UI and Export | 2/2 | Complete | 2026-04-12 |`
   - Phase 7 row: already shows Complete — verify it says `1/1 | Complete | 2026-04-12`
   - Phase 8 row: change from `0/1 | Not started` to `| 8. Phase 04 Verification | 1/1 | Complete | 2026-04-12 |`
   - Phase 9 row: update to `| 9. Requirements and Roadmap Documentation Sync | 0/1 | In Progress | - |` (plan count updated)

5. Update Phase 6 detail section (per D-07) — change plan checkboxes from `[ ]` to `[x]`:
   - `- [x] 06-01-PLAN.md — CSS variables dark mode refactor, toggle button, FOUC prevention, favicon, title, Dashboard redesign`
   - `- [x] 06-02-PLAN.md — @react-pdf/renderer install, TransferReportPDF component, Export PDF button in MatchPage`

6. Update Phase 8 detail section — change plan checkbox:
   - `- [x] 08-01-PLAN.md — Create 04-VERIFICATION.md with evidence for all 8 requirements and update REQUIREMENTS.md checkboxes`

7. Update Phase 9 detail section:
   - Change `**Plans**: 0 plans` to `**Plans**: 1 plan`
   - Add plan list: `- [ ] 09-01-PLAN.md — Build verification and documentation sync for 7 requirements`

8. Update Phase 9 header line from `- [ ]` to remain `- [ ]` (not yet complete, in progress).

9. Update the Execution Order line to include all phases: `Phases execute in numeric order: 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7 -> 8 -> 9`
  </action>
  <verify>
    <automated>grep -c "\- \[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains exactly 0 occurrences of `- [ ] **BILLING` (all 4 checked)
    - REQUIREMENTS.md contains exactly 0 occurrences of `- [ ] **BRAND` (both checked)
    - REQUIREMENTS.md contains exactly 0 occurrences of `- [ ] **RESULTS` (checked)
    - REQUIREMENTS.md traceability table contains 0 occurrences of `Pending`
    - REQUIREMENTS.md traceability table contains 26 occurrences of `Complete`
    - ROADMAP.md progress table row for Phase 8 contains `1/1 | Complete | 2026-04-12`
    - ROADMAP.md Phase 6 detail section contains `[x] 06-01-PLAN.md` and `[x] 06-02-PLAN.md`
    - ROADMAP.md Phase 9 detail section contains `**Plans**: 1 plan`
  </acceptance_criteria>
  <done>All 26 v1 requirements show [x] and Complete in REQUIREMENTS.md; ROADMAP.md progress table and plan checkboxes accurately reflect completed state for phases 4-8</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy | Incorrect checkbox state could mislead future planning agents into re-implementing completed work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | REQUIREMENTS.md | accept | No secrets in documentation files; all content is project-internal planning metadata |
| T-09-02 | Tampering | Traceability table | mitigate | Verify each checkbox against VERIFICATION.md evidence before marking Complete; build must exit 0 before RESULTS-02 is marked |
| T-09-03 | Denial of Service | Misleading docs | mitigate | Task 2 cross-references verification files to ensure accuracy; grep-based acceptance criteria catch any missed updates |
</threat_model>

<verification>
1. `cd apps/web && npm run build` exits 0
2. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
3. `grep -c "\- \[ \] \*\*BILLING" .planning/REQUIREMENTS.md` returns 0
4. `grep -c "\- \[ \] \*\*BRAND" .planning/REQUIREMENTS.md` returns 0
5. `grep -c "\- \[ \] \*\*RESULTS" .planning/REQUIREMENTS.md` returns 0
6. `grep "Phase 8" .planning/ROADMAP.md` shows Complete
</verification>

<success_criteria>
- All 26 v1 requirements show `[x]` checkbox and `Complete` in traceability table
- ROADMAP.md progress table accurate for all 9 phases
- apps/web builds successfully (RESULTS-02 confirmed)
- No `Pending` status remains in REQUIREMENTS.md traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/09-docs-sync/09-01-SUMMARY.md`
</output>

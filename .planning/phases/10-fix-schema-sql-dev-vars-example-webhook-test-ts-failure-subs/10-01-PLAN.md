---
phase: 10-fix-schema-sql-dev-vars-example-webhook-test-ts-failure-subs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/db/schema.sql
  - apps/worker/.dev.vars.example
  - apps/worker/wrangler.jsonc
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "schema.sql stores table includes store_number TEXT column"
    - "schema.sql subscriptions table DEFAULT is 'free' not 'inactive'"
    - ".dev.vars.example lists all 7 keys from types.ts Env interface"
    - "wrangler.jsonc comments list all 7 wrangler secret put commands"
    - "ROADMAP.md Phase 9 row shows 1/1 Complete"
    - "All 89 existing tests still pass with zero regressions"
  artifacts:
    - path: "apps/worker/src/db/schema.sql"
      provides: "Corrected stores table with store_number, corrected subscriptions DEFAULT"
      contains: "store_number TEXT"
    - path: "apps/worker/.dev.vars.example"
      provides: "Complete env var template for new developers"
      contains: "STRIPE_SECRET_KEY"
    - path: "apps/worker/wrangler.jsonc"
      provides: "Complete secret put comment list"
      contains: "STRIPE_SECRET_KEY"
  key_links:
    - from: "apps/worker/src/db/schema.sql"
      to: "apps/worker/src/routes/upload.ts"
      via: "store_number column referenced in INSERT and SELECT"
      pattern: "store_number"
    - from: "apps/worker/.dev.vars.example"
      to: "apps/worker/src/types.ts"
      via: "Env interface defines required keys"
      pattern: "STRIPE_SECRET_KEY.*STRIPE_WEBHOOK_SECRET.*STRIPE_PRICE_ID"
---

<objective>
Close 4 tech-debt items from the v1.0 milestone audit: add missing store_number column to schema.sql stores table, fix subscriptions.status DEFAULT from 'inactive' to 'free', add 3 missing Stripe keys to .dev.vars.example and wrangler.jsonc comments, and close ROADMAP.md Phase 9 status.

Purpose: A developer provisioning a fresh NEON database from schema.sql gets a working schema, and a new developer cloning the repo gets a complete .dev.vars.example with all required keys.
Output: 4 corrected files, 89/89 tests still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fix-schema-sql-dev-vars-example-webhook-test-ts-failure-subs/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix schema.sql — add store_number column and change subscriptions DEFAULT</name>
  <files>apps/worker/src/db/schema.sql</files>
  <read_first>
    - apps/worker/src/db/schema.sql (current state — stores table lines 19-25, subscriptions table lines 58-65)
    - apps/worker/src/db/migrations/001-add-store-number.sql (confirms store_number TEXT with no DEFAULT)
    - apps/worker/src/routes/upload.ts (references store_number in INSERT and SELECT queries)
    - apps/worker/src/routes/webhook.ts (writes 'paid' and 'free' to subscriptions.status — never 'inactive')
  </read_first>
  <action>
Edit `apps/worker/src/db/schema.sql` with two changes:

**Change 1 — stores table (lines 19-25):** Add `store_number TEXT` between the `name` and `created_at` columns. The complete stores CREATE TABLE must become:

```sql
CREATE TABLE IF NOT EXISTS stores (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id       TEXT NOT NULL REFERENCES orgs(org_id) ON DELETE CASCADE,
  name         TEXT NOT NULL,
  store_number TEXT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(org_id, name)
);
```

Note: `store_number TEXT` is nullable with no DEFAULT — matches the migration file `001-add-store-number.sql` and `upload.ts` which sets `storeNumber || null`.

**Change 2 — subscriptions table (line 63):** Change `DEFAULT 'inactive'` to `DEFAULT 'free'`. The line must become:

```sql
  status                  TEXT NOT NULL DEFAULT 'free',
```

Do NOT change any other lines in schema.sql. Do NOT add ALTER TABLE statements. Do NOT touch the indexes, RLS policies, or grants sections.
  </action>
  <verify>
    <automated>grep "store_number" apps/worker/src/db/schema.sql && grep "DEFAULT 'free'" apps/worker/src/db/schema.sql && ! grep "DEFAULT 'inactive'" apps/worker/src/db/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "store_number TEXT" apps/worker/src/db/schema.sql` returns 1
    - `grep "DEFAULT 'free'" apps/worker/src/db/schema.sql` matches the subscriptions status line
    - `grep "DEFAULT 'inactive'" apps/worker/src/db/schema.sql` returns no matches (exit code 1)
    - The stores CREATE TABLE has exactly 6 column lines: id, org_id, name, store_number, created_at, UNIQUE
    - No ALTER TABLE statements exist in schema.sql
  </acceptance_criteria>
  <done>schema.sql stores table includes store_number TEXT column; subscriptions.status DEFAULT is 'free'</done>
</task>

<task type="auto">
  <name>Task 2: Add Stripe keys to .dev.vars.example and wrangler.jsonc comments</name>
  <files>apps/worker/.dev.vars.example, apps/worker/wrangler.jsonc</files>
  <read_first>
    - apps/worker/.dev.vars.example (current state — 4 keys only)
    - apps/worker/wrangler.jsonc (current state — 4 secret put comments)
    - apps/worker/src/types.ts (authoritative Env interface — 7 required keys)
  </read_first>
  <action>
**Change 1 — .dev.vars.example:** Append the following 3 lines after the existing `ALLOWED_ORIGIN` line (after the blank line at end of file):

```
# Stripe — required for freemium billing (Phase 5)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_ID=price_xxx
```

The complete file must be exactly:
```
CLERK_SECRET_KEY=sk_test_xxx
CLERK_PUBLISHABLE_KEY=pk_test_xxx
DATABASE_URL=postgresql://pharmiq_app:xxx@xxx.neon.tech/neondb?sslmode=require
# ALLOWED_ORIGIN is used for CORS AND for azp (authorized parties) JWT claim validation.
# In production, set this to the Cloudflare Pages URL (e.g. https://pharmiq-stock-transfer-web.pages.dev).
# Without this, Clerk's @clerk/backend will reject tokens with an "Invalid azp" error (see RESEARCH Pitfall 6).
ALLOWED_ORIGIN=http://localhost:5173
# Stripe — required for freemium billing (Phase 5)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_ID=price_xxx
```

Do NOT add STRIPE_PRODCUT_ID_* keys (those are typo'd legacy keys not in types.ts Env interface). Do NOT add STRIPE_PUBLISHABLE_KEY (not in types.ts Env interface).

**Change 2 — wrangler.jsonc:** Add 3 new comment lines after the existing `ALLOWED_ORIGIN` line. The comments section must become:

```jsonc
// Secrets managed via `wrangler secret put` (not in config):
// wrangler secret put CLERK_SECRET_KEY
// wrangler secret put CLERK_PUBLISHABLE_KEY
// wrangler secret put DATABASE_URL
// wrangler secret put ALLOWED_ORIGIN
// wrangler secret put STRIPE_SECRET_KEY
// wrangler secret put STRIPE_WEBHOOK_SECRET
// wrangler secret put STRIPE_PRICE_ID
```
  </action>
  <verify>
    <automated>grep "STRIPE_SECRET_KEY" apps/worker/.dev.vars.example && grep "STRIPE_WEBHOOK_SECRET" apps/worker/.dev.vars.example && grep "STRIPE_PRICE_ID" apps/worker/.dev.vars.example && grep "STRIPE_SECRET_KEY" apps/worker/wrangler.jsonc && grep "STRIPE_WEBHOOK_SECRET" apps/worker/wrangler.jsonc && grep "STRIPE_PRICE_ID" apps/worker/wrangler.jsonc</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STRIPE_" apps/worker/.dev.vars.example` returns 3 (exactly 3 Stripe keys)
    - `grep "STRIPE_SECRET_KEY=sk_test_xxx" apps/worker/.dev.vars.example` matches
    - `grep "STRIPE_WEBHOOK_SECRET=whsec_xxx" apps/worker/.dev.vars.example` matches
    - `grep "STRIPE_PRICE_ID=price_xxx" apps/worker/.dev.vars.example` matches
    - `grep -c "STRIPE_" apps/worker/wrangler.jsonc` returns 3 (exactly 3 Stripe secret put lines)
    - `grep "PRODCUT" apps/worker/.dev.vars.example` returns no matches (no typo'd legacy keys)
    - Total key count in .dev.vars.example is 7 (matching types.ts Env interface exactly)
  </acceptance_criteria>
  <done>.dev.vars.example has all 7 Env keys with placeholder values; wrangler.jsonc lists all 7 secrets in comments</done>
</task>

<task type="auto">
  <name>Task 3: Close ROADMAP.md Phase 9 status and run full test suite</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 9 progress row shows "0/1 | In Progress | -")
  </read_first>
  <action>
**Change 1 — ROADMAP.md Progress table:** Find the Phase 9 row in the Progress table (near line 253):

```
| 9. Requirements and Roadmap Documentation Sync | 0/1 | In Progress | - |
```

Change it to:

```
| 9. Requirements and Roadmap Documentation Sync | 1/1 | Complete | 2026-04-13 |
```

**Change 2 — ROADMAP.md Phase 9 plan checkbox:** Find the Phase 9 plan list item:

```
- [ ] **Phase 9: Requirements and Roadmap Documentation Sync**
```

Change it to:

```
- [x] **Phase 9: Requirements and Roadmap Documentation Sync**
```

**Change 3 — ROADMAP.md Phase 10 entry:** Update the Phase 10 header block:
- Goal: Change `[To be planned]` to `Close tech-debt items from v1.0 milestone audit — schema.sql fixes, DX config completeness, and documentation sync`
- Plans: Change `0 plans` to `1 plan`
- Plan list: Change `- [ ] TBD (run /gsd-plan-phase 10 to break down)` to `- [ ] 10-01-PLAN.md — Schema fixes, DX config completeness, and Phase 9 close`

**Change 4 — Run full test suite:** After all file edits, run `cd apps/worker && rtk npm test` to confirm 89/89 tests pass with zero regressions. No TypeScript source files were changed, so all tests must pass identically.
  </action>
  <verify>
    <automated>cd apps/worker && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "1/1 | Complete | 2026-04-13" .planning/ROADMAP.md` matches the Phase 9 row
    - `grep "\[x\].*Phase 9" .planning/ROADMAP.md` matches
    - `grep "1 plan" .planning/ROADMAP.md` appears in Phase 10 block
    - `cd apps/worker && npm test` exits 0 with 89 tests passing
  </acceptance_criteria>
  <done>ROADMAP.md Phase 9 closed as Complete; Phase 10 goal and plan list populated; 89/89 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.sql -> NEON | Setup script defines table structure for fresh deployments |
| .dev.vars.example -> developer | Example file read by humans setting up local environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | .dev.vars.example | mitigate | File uses placeholder values (sk_test_xxx, whsec_xxx, price_xxx) — never real credentials. Verified by grep that no real key patterns exist. |
| T-10-02 | Tampering | schema.sql | accept | schema.sql is a setup script for fresh databases. Changes are additive (new column, DEFAULT value change). No impact on existing NEON deployments — migration 001 already handles store_number for existing DBs. |
</threat_model>

<verification>
1. `grep "store_number TEXT" apps/worker/src/db/schema.sql` returns a match
2. `grep "DEFAULT 'free'" apps/worker/src/db/schema.sql` returns a match on subscriptions status line
3. `grep -c "STRIPE_" apps/worker/.dev.vars.example` returns exactly 3
4. `grep -c "STRIPE_" apps/worker/wrangler.jsonc` returns exactly 3
5. `cd apps/worker && npm test` exits 0 with 89/89 tests passing
6. `grep "1/1 | Complete | 2026-04-13" .planning/ROADMAP.md` returns a match for Phase 9
</verification>

<success_criteria>
- schema.sql stores table has store_number TEXT column (matches migration 001 and upload.ts queries)
- schema.sql subscriptions.status DEFAULT is 'free' (matches webhook.ts two-state model)
- .dev.vars.example has all 7 keys from types.ts Env interface with placeholder values
- wrangler.jsonc comments list all 7 wrangler secret put commands
- ROADMAP.md Phase 9 row shows 1/1 Complete 2026-04-13
- ROADMAP.md Phase 10 goal and plan list are populated
- 89/89 tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-schema-sql-dev-vars-example-webhook-test-ts-failure-subs/10-01-SUMMARY.md`
</output>

---
phase: 11-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/db/migrations/002-v1.1-schema.sql
  - apps/worker/src/db/schema.sql
  - apps/worker/.dev.vars.example
  - apps/worker/wrangler.jsonc
autonomous: false
requirements: []
user_setup:
  - service: neon
    why: "Run v1.1 DDL migration against live NEON Postgres database before any v1.1 Worker code is deployed"
    env_vars:
      - name: DATABASE_URL
        source: ".dev.vars (already present) — must use neondb_owner role for DDL, not pharmiq_app"
    dashboard_config:
      - task: "Execute migrations/002-v1.1-schema.sql against live DB"
        location: "NEON SQL Editor (console.neon.tech → project → SQL Editor) OR psql with DATABASE_URL"

must_haves:
  truths:
    - "dead_stock table has a cost_ex column of type DOUBLE PRECISION in live NEON"
    - "subscriptions table has a plan_tier column of type TEXT NOT NULL DEFAULT 'free' in live NEON"
    - "subscriptions table has a stripe_price_id column of type TEXT (nullable) in live NEON"
    - "No subscription row exists where status='paid' AND plan_tier='free' after migration"
    - "schema.sql in the repo matches the post-migration live NEON schema (fresh-install equivalence)"
    - ".dev.vars.example lists STRIPE_PRICE_ID_PRO and STRIPE_PRICE_ID_ENTERPRISE placeholders"
    - "wrangler.jsonc comment block lists wrangler secret put commands for both new price IDs"
  artifacts:
    - path: "apps/worker/src/db/migrations/002-v1.1-schema.sql"
      provides: "Idempotent v1.1 schema migration DDL (cost_ex, plan_tier, stripe_price_id, paid→pro UPDATE)"
      contains: "ADD COLUMN IF NOT EXISTS cost_ex DOUBLE PRECISION"
    - path: "apps/worker/src/db/schema.sql"
      provides: "Canonical DDL for fresh installs — v1.1 columns reflected in CREATE TABLE blocks"
      contains: "cost_ex"
    - path: "apps/worker/.dev.vars.example"
      provides: "Reference secrets inventory — v1.1 Stripe price ID placeholders"
      contains: "STRIPE_PRICE_ID_PRO"
    - path: "apps/worker/wrangler.jsonc"
      provides: "Operator-facing list of secrets to run wrangler secret put for — v1.1 additions"
      contains: "STRIPE_PRICE_ID_PRO"
  key_links:
    - from: "apps/worker/src/db/schema.sql"
      to: "apps/worker/src/db/migrations/002-v1.1-schema.sql"
      via: "both define the same columns — schema.sql is the canonical fresh-install; migration is the delta"
      pattern: "cost_ex DOUBLE PRECISION"
    - from: "apps/worker/.dev.vars.example"
      to: "apps/worker/wrangler.jsonc"
      via: "same two secret names appear in both (.dev.vars for local dev, wrangler secret put for prod)"
      pattern: "STRIPE_PRICE_ID_(PRO|ENTERPRISE)"
---

<objective>
Phase 11 is a pure database-migration phase. It adds two columns to the live NEON database required by all v1.1 downstream phases, synchronises the canonical `schema.sql` in the repo, and registers the two new Stripe price ID secrets that Phase 15 will reference.

Purpose: v1.1 features (COST-*, BILLING-05..12) read from columns that do not yet exist. If any v1.1 Worker code is deployed before this migration runs, every affected request errors with `column "cost_ex" does not exist` or `column "plan_tier" does not exist`. Phase 11 must complete and be verified before any other v1.1 phase executes.

Output:
- `apps/worker/src/db/migrations/002-v1.1-schema.sql` (new migration file)
- `apps/worker/src/db/schema.sql` (updated canonical DDL)
- `apps/worker/.dev.vars.example` (updated secrets inventory)
- `apps/worker/wrangler.jsonc` (updated operator comment block)
- Live NEON database with `dead_stock.cost_ex`, `subscriptions.plan_tier`, `subscriptions.stripe_price_id`, and any `status='paid'` rows migrated to `plan_tier='pro'`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-migration/11-RESEARCH.md
@.planning/phases/11-schema-migration/11-VALIDATION.md

<!-- Prior precedent for migration pattern — read before writing 002-v1.1-schema.sql -->
@apps/worker/src/db/migrations/001-add-store-number.sql

<!-- Canonical DDL source of truth — this file must be edited in Task 3 -->
@apps/worker/src/db/schema.sql

<!-- Current secrets inventory — must be extended in Task 4 -->
@apps/worker/.dev.vars.example
@apps/worker/wrangler.jsonc

<interfaces>
<!-- Current schema.sql table definitions for the two affected tables (verbatim from apps/worker/src/db/schema.sql) -->

CREATE TABLE IF NOT EXISTS dead_stock (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id      TEXT NOT NULL,
  store_id    UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  sku         TEXT NOT NULL,
  description TEXT,
  soh         DOUBLE PRECISION,
  is_ranged   BOOLEAN NOT NULL DEFAULT false,
  uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS subscriptions (
  id                      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id                  TEXT NOT NULL REFERENCES orgs(org_id) ON DELETE CASCADE UNIQUE,
  stripe_customer_id      TEXT,
  stripe_subscription_id  TEXT,
  status                  TEXT NOT NULL DEFAULT 'free',
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT now()
);

<!-- Target schema.sql table definitions AFTER this phase (verbatim per 11-RESEARCH.md) -->

CREATE TABLE IF NOT EXISTS dead_stock (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id      TEXT NOT NULL,
  store_id    UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  sku         TEXT NOT NULL,
  description TEXT,
  soh         DOUBLE PRECISION,
  cost_ex     DOUBLE PRECISION,
  is_ranged   BOOLEAN NOT NULL DEFAULT false,
  uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS subscriptions (
  id                      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id                  TEXT NOT NULL REFERENCES orgs(org_id) ON DELETE CASCADE UNIQUE,
  stripe_customer_id      TEXT,
  stripe_subscription_id  TEXT,
  stripe_price_id         TEXT,
  status                  TEXT NOT NULL DEFAULT 'free',
  plan_tier               TEXT NOT NULL DEFAULT 'free',
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT now()
);

<!-- Phase 3 migration precedent (verbatim from apps/worker/src/db/migrations/001-add-store-number.sql) -->
-- Phase 3 migration: add store_number column to stores table (per CONTEXT.md D-01, D-03)
ALTER TABLE stores ADD COLUMN IF NOT EXISTS store_number TEXT;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration SQL file 002-v1.1-schema.sql</name>
  <files>apps/worker/src/db/migrations/002-v1.1-schema.sql</files>
  <read_first>
    - apps/worker/src/db/migrations/001-add-store-number.sql (Phase 3 precedent for migration-file format and header-comment style)
    - apps/worker/src/db/schema.sql (current dead_stock and subscriptions table definitions)
    - .planning/phases/11-schema-migration/11-RESEARCH.md (Pitfalls 1, 2, 4 — NOT NULL/DEFAULT, cost_ex type, paid→pro UPDATE)
  </read_first>
  <action>
Create the file `apps/worker/src/db/migrations/002-v1.1-schema.sql` with the exact contents below. Do not substitute, reorder, or rename anything. All four statements must appear in this order so that the column exists before the UPDATE runs.

```sql
-- Phase 11 migration: v1.1 schema additions
-- Adds cost_ex to dead_stock, plan_tier + stripe_price_id to subscriptions,
-- and migrates any existing paid subscription rows to plan_tier='pro'.
--
-- Idempotency: all statements use IF NOT EXISTS so this file is safe to re-run.
-- The UPDATE at the end is idempotent by construction (setting pro->pro is a no-op).
--
-- Run as `neondb_owner` role (DDL requires ownership). Application queries continue
-- to use `pharmiq_app` role (BYPASSRLS=false). See schema.sql lines 3-4.
--
-- Execution: NEON SQL Editor (console.neon.tech → SQL Editor) OR
--            psql "$DATABASE_URL" -f apps/worker/src/db/migrations/002-v1.1-schema.sql

-- 1. Add cost_ex to dead_stock (nullable — absent from pre-v1.1 uploads)
ALTER TABLE dead_stock
  ADD COLUMN IF NOT EXISTS cost_ex DOUBLE PRECISION;

-- 2. Add plan_tier to subscriptions (NOT NULL with DEFAULT so existing rows get 'free')
ALTER TABLE subscriptions
  ADD COLUMN IF NOT EXISTS plan_tier TEXT NOT NULL DEFAULT 'free';

-- 3. Add stripe_price_id to subscriptions (nullable — set at checkout or webhook time)
ALTER TABLE subscriptions
  ADD COLUMN IF NOT EXISTS stripe_price_id TEXT;

-- 4. Migrate existing paid rows to pro tier (no-op if no paid rows exist)
UPDATE subscriptions
  SET plan_tier = 'pro'
  WHERE status = 'paid';
```

Concrete values to honour:
- File path EXACTLY: `apps/worker/src/db/migrations/002-v1.1-schema.sql`
- `cost_ex` type EXACTLY: `DOUBLE PRECISION` (NOT `TEXT`, NOT `NUMERIC`, NOT `FLOAT`) — see 11-RESEARCH.md Pitfall 2
- `plan_tier` MUST be `TEXT NOT NULL DEFAULT 'free'` — the DEFAULT is required because existing rows have no value (Pitfall 1)
- `stripe_price_id` MUST be `TEXT` (nullable, no default) — set only at checkout/webhook time
- The UPDATE MUST be the last statement in the file so the column exists before it runs (Pitfall 4)
- Every ALTER uses `IF NOT EXISTS` — the migration is safe to re-run

Do not create, add, or modify any other file in this task.
  </action>
  <verify>
    <automated>test -f apps/worker/src/db/migrations/002-v1.1-schema.sql &amp;&amp; grep -c "ADD COLUMN IF NOT EXISTS cost_ex DOUBLE PRECISION" apps/worker/src/db/migrations/002-v1.1-schema.sql &amp;&amp; grep -c "ADD COLUMN IF NOT EXISTS plan_tier TEXT NOT NULL DEFAULT 'free'" apps/worker/src/db/migrations/002-v1.1-schema.sql &amp;&amp; grep -c "ADD COLUMN IF NOT EXISTS stripe_price_id TEXT" apps/worker/src/db/migrations/002-v1.1-schema.sql &amp;&amp; grep -c "UPDATE subscriptions" apps/worker/src/db/migrations/002-v1.1-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/worker/src/db/migrations/002-v1.1-schema.sql` exists
    - File contains literal string `ADD COLUMN IF NOT EXISTS cost_ex DOUBLE PRECISION` (exactly one occurrence)
    - File contains literal string `ADD COLUMN IF NOT EXISTS plan_tier TEXT NOT NULL DEFAULT 'free'` (exactly one occurrence)
    - File contains literal string `ADD COLUMN IF NOT EXISTS stripe_price_id TEXT` (exactly one occurrence)
    - File contains literal string `UPDATE subscriptions` (exactly one occurrence)
    - File contains literal string `SET plan_tier = 'pro'` (exactly one occurrence)
    - File contains literal string `WHERE status = 'paid'` (exactly one occurrence)
    - File does NOT contain the string `cost_ex TEXT` (wrong type — forbidden)
    - File does NOT contain the string `plan_tier TEXT NOT NULL` without a DEFAULT clause (forbidden — would fail on rows that already exist)
    - The UPDATE statement appears AFTER all three ADD COLUMN statements (line number check: `grep -n "UPDATE subscriptions" FILE` returns a line number greater than the line number of `grep -n "ADD COLUMN IF NOT EXISTS plan_tier" FILE`)
  </acceptance_criteria>
  <done>Migration file exists at the exact path above with all four statements in order, using the exact types and constraints from the interfaces block.</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical schema.sql with the three new columns</name>
  <files>apps/worker/src/db/schema.sql</files>
  <read_first>
    - apps/worker/src/db/schema.sql (current dead_stock and subscriptions CREATE TABLE blocks — modify in place)
    - .planning/phases/11-schema-migration/11-RESEARCH.md (Pitfall 5 — forgetting to update schema.sql breaks fresh installs)
    - apps/worker/src/db/migrations/002-v1.1-schema.sql (created in Task 1 — schema.sql must match the post-migration state of live NEON exactly)
  </read_first>
  <action>
Edit `apps/worker/src/db/schema.sql` in place. Make exactly TWO edits — one to the `dead_stock` CREATE TABLE block and one to the `subscriptions` CREATE TABLE block. Do not modify any other part of the file (orgs, stores, rou_data, usage_meters, indexes, RLS, grants must remain byte-identical).

Edit 1 — `dead_stock` CREATE TABLE block:

Find the existing block (current text verbatim):

```sql
CREATE TABLE IF NOT EXISTS dead_stock (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id      TEXT NOT NULL,
  store_id    UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  sku         TEXT NOT NULL,
  description TEXT,
  soh         DOUBLE PRECISION,
  is_ranged   BOOLEAN NOT NULL DEFAULT false,
  uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Replace with (note: the ONLY change is adding the `cost_ex` line between `soh` and `is_ranged`):

```sql
CREATE TABLE IF NOT EXISTS dead_stock (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id      TEXT NOT NULL,
  store_id    UUID NOT NULL REFERENCES stores(id) ON DELETE CASCADE,
  sku         TEXT NOT NULL,
  description TEXT,
  soh         DOUBLE PRECISION,
  cost_ex     DOUBLE PRECISION,
  is_ranged   BOOLEAN NOT NULL DEFAULT false,
  uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Edit 2 — `subscriptions` CREATE TABLE block:

Find the existing block (current text verbatim):

```sql
CREATE TABLE IF NOT EXISTS subscriptions (
  id                      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id                  TEXT NOT NULL REFERENCES orgs(org_id) ON DELETE CASCADE UNIQUE,
  stripe_customer_id      TEXT,
  stripe_subscription_id  TEXT,
  status                  TEXT NOT NULL DEFAULT 'free',
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Replace with (two new lines inserted: `stripe_price_id` between `stripe_subscription_id` and `status`; `plan_tier` between `status` and `updated_at`):

```sql
CREATE TABLE IF NOT EXISTS subscriptions (
  id                      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id                  TEXT NOT NULL REFERENCES orgs(org_id) ON DELETE CASCADE UNIQUE,
  stripe_customer_id      TEXT,
  stripe_subscription_id  TEXT,
  stripe_price_id         TEXT,
  status                  TEXT NOT NULL DEFAULT 'free',
  plan_tier               TEXT NOT NULL DEFAULT 'free',
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Column alignment: match the existing alignment convention already used in each CREATE TABLE block (the `dead_stock` block uses a single space after the column name padding to a short width; the `subscriptions` block uses padding to `stripe_subscription_id` width). Copy the padding from adjacent rows.

Do not modify: the `orgs` table, the `stores` table, the `rou_data` table, the `usage_meters` table, any CREATE INDEX, any ALTER TABLE ... ENABLE ROW LEVEL SECURITY, any CREATE POLICY, any GRANT, the role-creation DO block, or the file header comment. Those must remain byte-identical.
  </action>
  <verify>
    <automated>grep -c "cost_ex     DOUBLE PRECISION" apps/worker/src/db/schema.sql &amp;&amp; grep -c "plan_tier               TEXT NOT NULL DEFAULT 'free'" apps/worker/src/db/schema.sql &amp;&amp; grep -c "stripe_price_id         TEXT" apps/worker/src/db/schema.sql &amp;&amp; grep -cE "CREATE TABLE IF NOT EXISTS (orgs|stores|rou_data|dead_stock|usage_meters|subscriptions)" apps/worker/src/db/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cost_ex" apps/worker/src/db/schema.sql` returns 1 (exactly one occurrence)
    - `grep -c "plan_tier" apps/worker/src/db/schema.sql` returns 1 (exactly one occurrence)
    - `grep -c "stripe_price_id" apps/worker/src/db/schema.sql` returns 1 (exactly one occurrence)
    - `grep -cE "^CREATE TABLE IF NOT EXISTS (orgs|stores|rou_data|dead_stock|usage_meters|subscriptions)" apps/worker/src/db/schema.sql` returns 6 (all six tables still present)
    - The line containing `cost_ex` appears AFTER the line containing `soh         DOUBLE PRECISION,` inside the `dead_stock` block and BEFORE `is_ranged   BOOLEAN NOT NULL DEFAULT false,`
    - The line containing `plan_tier` appears AFTER the line containing `status                  TEXT NOT NULL DEFAULT 'free',` and BEFORE `updated_at              TIMESTAMPTZ NOT NULL DEFAULT now()` inside the `subscriptions` block
    - The line containing `stripe_price_id` appears AFTER `stripe_subscription_id  TEXT,` and BEFORE `status                  TEXT NOT NULL DEFAULT 'free',` inside the `subscriptions` block
    - `grep -c "CREATE POLICY org_isolation" apps/worker/src/db/schema.sql` returns 6 (all RLS policies preserved)
    - `grep -c "GRANT SELECT, INSERT, UPDATE, DELETE" apps/worker/src/db/schema.sql` returns 1 (grant preserved)
  </acceptance_criteria>
  <done>schema.sql contains cost_ex in dead_stock and both plan_tier + stripe_price_id in subscriptions, with all RLS, grants, indexes, and other tables unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Update .dev.vars.example and wrangler.jsonc with STRIPE_PRICE_ID_PRO and STRIPE_PRICE_ID_ENTERPRISE</name>
  <files>apps/worker/.dev.vars.example, apps/worker/wrangler.jsonc</files>
  <read_first>
    - apps/worker/.dev.vars.example (current secrets inventory — append new entries after the existing STRIPE_PRICE_ID line)
    - apps/worker/wrangler.jsonc (current operator comment block — append new wrangler secret put entries after the existing STRIPE_PRICE_ID comment)
    - .planning/phases/11-schema-migration/11-RESEARCH.md (section "Runtime State Inventory" confirms existing STRIPE_PRICE_ID remains and two new price IDs are added)
  </read_first>
  <action>
Make two concurrent file edits. Keep the existing `STRIPE_PRICE_ID=price_xxx` line and existing `wrangler secret put STRIPE_PRICE_ID` comment exactly as-is — they are still used by Phase 5 billing code and will be deprecated in Phase 15, not here.

Edit 1 — `apps/worker/.dev.vars.example`:

The current file ends with:
```
STRIPE_PRICE_ID=price_xxx
```

Append the following lines AFTER that line (and a trailing newline at end of file). Do not modify any line above:

```
# Phase 11 (v1.1): Stripe price IDs for Pro and Enterprise tiers (required by Phase 15)
# Create these prices in the Stripe Dashboard → Products → Add Product:
#   - PharmIQ Pro: $10 AUD/month recurring → save price ID as STRIPE_PRICE_ID_PRO
#   - PharmIQ Enterprise: $100 AUD/month recurring → save price ID as STRIPE_PRICE_ID_ENTERPRISE
# Then register them as Worker secrets:
#   wrangler secret put STRIPE_PRICE_ID_PRO
#   wrangler secret put STRIPE_PRICE_ID_ENTERPRISE
STRIPE_PRICE_ID_PRO=price_xxx
STRIPE_PRICE_ID_ENTERPRISE=price_xxx
```

Edit 2 — `apps/worker/wrangler.jsonc`:

The current file ends its comment block with:
```
// wrangler secret put STRIPE_PRICE_ID
```

Append the following lines AFTER that line (and a trailing newline at end of file). Do not modify the JSON block above the comment block:

```
// wrangler secret put STRIPE_PRICE_ID_PRO         // Phase 11 (v1.1)
// wrangler secret put STRIPE_PRICE_ID_ENTERPRISE  // Phase 11 (v1.1)
```

Concrete values to honour:
- Secret names EXACTLY as written (all uppercase, underscores): `STRIPE_PRICE_ID_PRO`, `STRIPE_PRICE_ID_ENTERPRISE`
- Placeholder value in .dev.vars.example EXACTLY: `price_xxx` (the literal string used for all other Stripe placeholders in this file — never a real price ID)
- Do NOT remove or rename the existing `STRIPE_PRICE_ID=price_xxx` line in .dev.vars.example — it is still consumed by the Phase 5 billing route until Phase 15 replaces it
- Do NOT remove or rename the existing `// wrangler secret put STRIPE_PRICE_ID` comment in wrangler.jsonc — same reason
  </action>
  <verify>
    <automated>grep -c "^STRIPE_PRICE_ID_PRO=price_xxx$" apps/worker/.dev.vars.example &amp;&amp; grep -c "^STRIPE_PRICE_ID_ENTERPRISE=price_xxx$" apps/worker/.dev.vars.example &amp;&amp; grep -c "^STRIPE_PRICE_ID=price_xxx$" apps/worker/.dev.vars.example &amp;&amp; grep -c "wrangler secret put STRIPE_PRICE_ID_PRO" apps/worker/wrangler.jsonc &amp;&amp; grep -c "wrangler secret put STRIPE_PRICE_ID_ENTERPRISE" apps/worker/wrangler.jsonc &amp;&amp; grep -c "wrangler secret put STRIPE_PRICE_ID$" apps/worker/wrangler.jsonc</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^STRIPE_PRICE_ID_PRO=price_xxx$" apps/worker/.dev.vars.example` returns 1
    - `grep -c "^STRIPE_PRICE_ID_ENTERPRISE=price_xxx$" apps/worker/.dev.vars.example` returns 1
    - `grep -c "^STRIPE_PRICE_ID=price_xxx$" apps/worker/.dev.vars.example` returns 1 (existing line preserved, NOT removed)
    - `grep -c "STRIPE_PRICE_ID_PRO" apps/worker/wrangler.jsonc` returns 1 (exactly one occurrence — in the comment block)
    - `grep -c "STRIPE_PRICE_ID_ENTERPRISE" apps/worker/wrangler.jsonc` returns 1
    - `grep -c "wrangler secret put STRIPE_PRICE_ID$" apps/worker/wrangler.jsonc` returns 1 (existing line preserved)
    - `grep -c "^  \"name\": \"pharmiq-stock-transfer-worker\"" apps/worker/wrangler.jsonc` returns 1 (JSON block unchanged)
    - .dev.vars.example file does NOT contain any real Stripe price ID (no string matching pattern `price_1[A-Za-z0-9]{10,}` — only `price_xxx` placeholders)
  </acceptance_criteria>
  <done>Both files list the two new Stripe price ID placeholders alongside the existing v1.0 STRIPE_PRICE_ID entry.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Worker test suite still passes after schema.sql edit</name>
  <files>(no file writes — read-only verification)</files>
  <read_first>
    - apps/worker/src/db/schema.sql (confirm post-Task-2 state before running tests)
    - apps/worker/vitest.config.ts (confirm test config is unchanged)
    - .planning/phases/11-schema-migration/11-VALIDATION.md (Sampling Rate section — `cd apps/worker &amp;&amp; npm test` is the phase gate)
  </read_first>
  <action>
Run the Worker test suite from `apps/worker/` and confirm it exits 0. Tests mock the database; schema.sql is not read at test time, so the edit in Task 2 must not cause any regression. If any test fails, do NOT continue to the checkpoint — report the failure and stop.

Exact command:

```bash
cd apps/worker && npm test
```

Do not make any code or test changes. If tests were already failing before Phase 11 (pre-existing red), record that in the task summary but do not attempt fixes in this plan. Any test that was green before this plan must still be green.

Do not create or modify any file in this task.
  </action>
  <verify>
    <automated>cd apps/worker &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/worker &amp;&amp; npm test` exits with code 0
    - Vitest output contains the string `Test Files` (confirms Vitest ran, not a missing-script error)
    - No test that was passing before this plan reports as failing now
    - No new test file exists under `apps/worker/src/` or `apps/worker/test/` that was not present before this plan (Phase 11 adds no tests)
  </acceptance_criteria>
  <done>Full Worker test suite exits 0. No code regression introduced by the schema.sql edit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Execute migration SQL against live NEON database</name>
  <files>apps/worker/src/db/migrations/002-v1.1-schema.sql (read-only — executed against live NEON)</files>
  <read_first>
    - apps/worker/src/db/migrations/002-v1.1-schema.sql (the SQL to copy into the NEON SQL Editor or pipe into psql — created in Task 1)
    - apps/worker/.dev.vars (read DATABASE_URL — confirm it targets the correct NEON project and uses neondb_owner role)
    - .planning/phases/11-schema-migration/11-RESEARCH.md (Pitfalls 3 and 4 — deploying code before migration, and partial migration leaving paid rows)
  </read_first>
  <action>
This is the only non-automatable task in Phase 11. Claude cannot run DDL against NEON without a DB helper; the migration is executed by the user via the NEON SQL Editor (browser) or psql (terminal).

Migration file `apps/worker/src/db/migrations/002-v1.1-schema.sql` (committed in Task 1) contains the four DDL/DML statements required. This task walks the user through executing it and the five verification queries that confirm success.

Step 1 — Execute the migration. Pick ONE of these paths:

Path A (NEON SQL Editor — recommended):
1. Open https://console.neon.tech
2. Navigate to the PharmIQ project → SQL Editor
3. Ensure the connection role is `neondb_owner` (DDL requires ownership — `pharmiq_app` cannot run ALTER TABLE)
4. Copy the entire contents of `apps/worker/src/db/migrations/002-v1.1-schema.sql` into the editor
5. Click Run
6. Confirm four successful statements (three ALTER TABLE and one UPDATE)

Path B (psql from terminal):
1. Source `DATABASE_URL` from `apps/worker/.dev.vars` (confirm it uses `neondb_owner`, not `pharmiq_app`)
2. Run: `psql "$DATABASE_URL" -f apps/worker/src/db/migrations/002-v1.1-schema.sql`
3. Confirm no errors reported

Step 2 — Run these five verification queries and confirm each matches the expected output. Paste Query 5 output into the resume signal.

Query 1 — `d dead_stock` → expect row: `cost_ex | double precision | (nullable)`
Query 2 — `d subscriptions` → expect rows: `plan_tier | text | not null | 'free'::text` AND `stripe_price_id | text | (nullable)`
Query 3 — `SELECT status, plan_tier, COUNT(*) FROM subscriptions GROUP BY status, plan_tier;` → expect NO row with status='paid' AND plan_tier='free'
Query 4 — `SELECT COUNT(*) FROM subscriptions WHERE plan_tier IS NULL;` → expect 0
Query 5 — `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE (table_name = 'dead_stock' AND column_name = 'cost_ex') OR (table_name = 'subscriptions' AND column_name IN ('plan_tier', 'stripe_price_id')) ORDER BY table_name, column_name;` → expect three rows:
  - dead_stock    | cost_ex         | double precision | YES | NULL
  - subscriptions | plan_tier       | text             | NO  | 'free'::text
  - subscriptions | stripe_price_id | text             | YES | NULL

DO NOT deploy any v1.1 Worker code (Phase 12+) until this task is signed off. Deploying ahead of the migration produces `column "cost_ex" does not exist` errors on every affected request (11-RESEARCH.md Pitfall 3).
  </action>
  <verify>
    <automated>echo "MANUAL VERIFICATION: user must paste Query 5 output (three rows) into resume signal — no Worker-side automated verification possible for live NEON DDL state. Plan cannot auto-verify this task; downstream Phase 12 will surface any missed migration via test failure on first Phase 12 task."</automated>
    <manual>User pastes Query 5 output into resume signal. Expected exactly three rows: dead_stock.cost_ex (double precision, nullable), subscriptions.plan_tier (text, not null, default 'free'::text), subscriptions.stripe_price_id (text, nullable).</manual>
  </verify>
  <acceptance_criteria>
    - User responds to resume signal with Query 5 output OR literal string "migration-verified"
    - If Query 5 output pasted: contains the literal string "double precision" on a line mentioning "cost_ex"
    - If Query 5 output pasted: contains the literal string "not null" on a line mentioning "plan_tier"
    - If Query 5 output pasted: contains the literal string "'free'::text" (plan_tier default)
    - If Query 5 output pasted: contains a line mentioning "stripe_price_id" with "text" and with "YES" in the nullable column
    - User has NOT responded with "migration-failed" prefix (plan halts without commit if they do)
  </acceptance_criteria>
  <done>Live NEON database has cost_ex, plan_tier, and stripe_price_id columns in place; any paid subscription rows are migrated to plan_tier='pro'; user has confirmed Query 5 output matches the expected three rows.</done>
  <what-built>
    Migration file `apps/worker/src/db/migrations/002-v1.1-schema.sql` has been committed to the repo (Task 1) and contains the four DDL/DML statements required to add cost_ex, plan_tier, and stripe_price_id columns to the live NEON database and migrate any existing paid rows to the pro tier.

    This is a DDL operation against the live NEON production database. There is no Cloudflare API, no wrangler command, and no node-side helper that executes DDL on the user's behalf — the user must run the SQL through either the NEON SQL Editor (browser) or psql (terminal). This is the only step in Phase 11 that is not automatable by Claude.
  </what-built>
  <how-to-verify>
    Run Queries 1-5 above in the NEON SQL Editor or via psql. Expected outputs documented in the action block. Paste Query 5 output into the resume signal.
  </how-to-verify>
  <resume-signal>Paste the three-row output of Query 5 (or type "migration-verified: &lt;notes&gt;" if output was confirmed through the NEON UI instead of SQL). If any query returns unexpected results, type "migration-failed: &lt;what went wrong&gt;" and the plan will halt without committing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → NEON DDL session | DDL execution requires `neondb_owner` role (BYPASSRLS=true); application runtime uses `pharmiq_app` (BYPASSRLS=false). Misusing `neondb_owner` from a normal route would silently disable RLS. |
| Repo → secret inventory | `.dev.vars.example` and wrangler.jsonc are committed and therefore public. Real Stripe price IDs must never appear; only `price_xxx` placeholders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | Migration execution | mitigate | `002-v1.1-schema.sql` header comment explicitly documents "Run as `neondb_owner` role. Application queries continue to use `pharmiq_app`." No application query is changed — all existing GRANTs in schema.sql cover the new columns transparently (table-level GRANT covers all columns). |
| T-11-02 | Information Disclosure | `.dev.vars.example` commit | mitigate | Placeholder `price_xxx` hardcoded in plan text; Task 3 acceptance criterion forbids any string matching `price_1[A-Za-z0-9]{10,}` (real Stripe price ID pattern). `.dev.vars` itself remains gitignored. |
| T-11-03 | Tampering | Re-running migration accidentally | accept | All ALTER statements use `ADD COLUMN IF NOT EXISTS`; the UPDATE is idempotent (setting `plan_tier='pro'` on already-pro rows is a no-op). Re-execution by mistake cannot corrupt state. |
| T-11-04 | Denial of Service | Deploying Phase 12/15 code before migration | mitigate | Checkpoint Task 5 is a blocking gate — no further plans can run until migration is verified. Roadmap entry also documents the order: "Run the DB migration BEFORE deploying any v1.1 Worker code." |
| T-11-05 | Information Disclosure | schema.sql commit exposing column structure | accept | Column names and types are design-time public; RLS policies prevent cross-org reads regardless. |
| T-11-06 | Repudiation | Loss of migration audit trail | accept | Migration file is committed to git with a dated commit; NEON logs the DDL execution with timestamp. No additional audit infra required for a pre-launch pharmacy tool. |
| T-11-07 | Spoofing | N/A for DDL-only phase | n/a | No authentication surface changed. AUTH-01/02/03 boundaries from Phase 1 remain untouched. |
</threat_model>

<verification>
Phase 11 is complete when ALL of the following are true:

1. `apps/worker/src/db/migrations/002-v1.1-schema.sql` exists and passes Task 1 acceptance criteria.
2. `apps/worker/src/db/schema.sql` contains `cost_ex` in the `dead_stock` block and both `plan_tier` and `stripe_price_id` in the `subscriptions` block (Task 2 acceptance criteria).
3. `apps/worker/.dev.vars.example` and `apps/worker/wrangler.jsonc` both list the two new Stripe price ID entries (Task 3 acceptance criteria).
4. `cd apps/worker && npm test` exits 0 after the schema.sql edit (Task 4).
5. Live NEON database shows:
   - `\d dead_stock` includes `cost_ex | double precision | (nullable)`
   - `\d subscriptions` includes `plan_tier | text | not null | 'free'` and `stripe_price_id | text | (nullable)`
   - `SELECT status, plan_tier FROM subscriptions` has no rows with `status='paid' AND plan_tier='free'`
   - `SELECT COUNT(*) FROM subscriptions WHERE plan_tier IS NULL` returns 0
6. schema.sql matches the post-migration live NEON state (Task 5 Query 5 output rows align with the CREATE TABLE blocks in schema.sql).

Any verification failure blocks Phase 12 and Phase 15 from starting.
</verification>

<success_criteria>
- [x] Migration file exists at `apps/worker/src/db/migrations/002-v1.1-schema.sql` with all four statements
- [x] schema.sql reflects the post-migration state (cost_ex, plan_tier, stripe_price_id)
- [x] .dev.vars.example lists STRIPE_PRICE_ID_PRO and STRIPE_PRICE_ID_ENTERPRISE
- [x] wrangler.jsonc comment block lists `wrangler secret put` commands for both new secrets
- [x] Worker test suite exits 0 after schema.sql edit
- [x] Live NEON `\d dead_stock` shows `cost_ex double precision`
- [x] Live NEON `\d subscriptions` shows `plan_tier text not null default 'free'` and `stripe_price_id text`
- [x] No subscription row has `status='paid' AND plan_tier='free'`
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-migration/11-01-SUMMARY.md` documenting:
- Migration SQL file path and contents hash
- schema.sql diff (two CREATE TABLE blocks updated)
- .dev.vars.example and wrangler.jsonc additions
- Output of Task 5 verification queries (pasted from NEON SQL Editor or psql)
- Any paid rows that were migrated (count from `SELECT COUNT(*) FROM subscriptions WHERE status='paid'`)
- Confirmation that `npm test` exited 0 before and after the schema.sql edit
</output>

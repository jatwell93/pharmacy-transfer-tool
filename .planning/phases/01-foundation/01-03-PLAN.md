---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/worker/src/index.ts
  - apps/web/src/hooks/useFetch.ts
autonomous: false

requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

user_setup:
  - service: clerk
    why: "Authentication provider — must create Clerk application and configure org, social logins, JWT template before the app works"
    env_vars:
      - name: VITE_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> PharmIQ Stock Transfer app -> API Keys -> Publishable key"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> PharmIQ Stock Transfer app -> API Keys -> Secret key"
    dashboard_config:
      - task: "Create Clerk application named 'PharmIQ Stock Transfer'"
        location: "Clerk Dashboard -> Applications -> Create application"
      - task: "Enable Organizations"
        location: "Clerk Dashboard -> Organizations -> Settings -> Enable"
      - task: "Enable Google sign-in"
        location: "Clerk Dashboard -> User & Authentication -> Social Connections -> Google"
      - task: "Enable Microsoft (Azure AD) sign-in"
        location: "Clerk Dashboard -> User & Authentication -> Social Connections -> Microsoft"
      - task: "Create JWT template named 'stocktransfer-worker' with claims: { \"org_id\": \"{{org.id}}\" }"
        location: "Clerk Dashboard -> JWT Templates -> New template"
  - service: neon
    why: "Postgres database — must create NEON project and run schema migration"
    env_vars:
      - name: DATABASE_URL
        source: "NEON Console -> Connection Details -> Connection string (use pharmiq_app role, NOT neondb_owner)"
    dashboard_config:
      - task: "Create NEON project"
        location: "NEON Console -> New Project"
      - task: "Run schema.sql via SQL Editor to create tables, RLS, and pharmiq_app role"
        location: "NEON Console -> SQL Editor -> paste apps/worker/src/db/schema.sql"
      - task: "Create password for pharmiq_app role and note the connection string"
        location: "NEON Console -> SQL Editor: ALTER ROLE pharmiq_app LOGIN PASSWORD 'xxx'; then build connection string"
  - service: cloudflare
    why: "Workers Paid plan must be active before deploying the Worker"
    dashboard_config:
      - task: "Upgrade to Workers Paid plan ($5/month)"
        location: "Cloudflare Dashboard -> Workers & Pages -> Plans -> Upgrade"

must_haves:
  truths:
    - "User can sign in via Clerk and is redirected to the app dashboard"
    - "An authenticated API request to /api/health returns the caller's org_id from the verified JWT"
    - "A request with missing/invalid JWT is rejected with 401"
    - "A request from a user with no active Clerk org is rejected with 403"
    - "The SPA fetches from the Worker API with a Bearer token from Clerk"
  artifacts:
    - path: "apps/worker/src/index.ts"
      provides: "Worker entry with CORS allowing the Pages origin"
      contains: "ALLOWED_ORIGIN"
    - path: "apps/web/src/hooks/useFetch.ts"
      provides: "Auth-aware fetch hook pointing at Worker URL"
      contains: "VITE_WORKER_URL"
  key_links:
    - from: "apps/web/src/hooks/useFetch.ts"
      to: "apps/worker/src/index.ts"
      via: "Bearer token in Authorization header to /api/health"
      pattern: "Authorization.*Bearer"
    - from: "apps/worker/src/middleware/auth.ts"
      to: "Clerk JWKS"
      via: "@hono/clerk-auth verifies token signature against Clerk's JWKS endpoint with authorizedParties validation"
      pattern: "authorizedParties"
    - from: "apps/worker/src/db/client.ts"
      to: "NEON Postgres"
      via: "neon() HTTP driver with org_id injected via set_config for RLS"
      pattern: "set_config"
---

<objective>
Wire the Worker API and React SPA together, then verify the full auth flow end-to-end.

Purpose: Confirms that all Phase 1 success criteria are met — sign-in works, auth gates return correct status codes, org_id flows from JWT to API response, and NEON schema is deployed with RLS.

Output: A verified, deployable authenticated scaffold with both apps wired together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CORS, environment variables, and verify local dev connectivity</name>
  <files>
    apps/worker/src/index.ts,
    apps/web/src/hooks/useFetch.ts
  </files>
  <read_first>
    apps/worker/src/index.ts,
    apps/worker/src/middleware/auth.ts,
    apps/worker/wrangler.jsonc,
    apps/web/src/hooks/useFetch.ts,
    apps/web/src/App.tsx,
    .planning/phases/01-foundation/01-01-SUMMARY.md,
    .planning/phases/01-foundation/01-02-SUMMARY.md
  </read_first>
  <action>
Verify and finalize the integration wiring between Worker and Web:

1. **Worker CORS** — Confirm `apps/worker/src/index.ts` has CORS middleware configured to accept requests from the Pages origin. The CORS config should use `c.env.ALLOWED_ORIGIN` (set to `http://localhost:5173` in `.dev.vars` for local dev). Ensure:
   - `origin` callback returns the `ALLOWED_ORIGIN` value
   - `allowHeaders: ['Authorization', 'Content-Type']` is included
   - `allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS']` is included

   If not already present, update the cors middleware call in index.ts:
   ```typescript
   app.use('*', cors({
     origin: (origin, c) => c.env.ALLOWED_ORIGIN,
     allowHeaders: ['Authorization', 'Content-Type'],
     allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
   }));
   ```

2. **Web fetch hook** — Confirm `apps/web/src/hooks/useFetch.ts` correctly:
   - Calls `getToken()` from `useAuth()` to get Clerk session token
   - Prepends `import.meta.env.VITE_WORKER_URL` to the endpoint
   - Sets `Authorization: Bearer ${token}` header
   - Returns the fetch Response

3. **Local dev setup** — Create/update `apps/worker/.dev.vars` (gitignored) with placeholder values. Add `.dev.vars` to `apps/worker/.gitignore` if not already present. Ensure `apps/web/.env` (gitignored) has placeholder values matching `.env.example`.

4. **Smoke test** — After wiring, run `cd apps/worker && npm test -- --run` to ensure existing tests still pass. Run `cd apps/web && npx vite build` to ensure the web app still builds.
  </action>
  <verify>
    <automated>cd apps/worker && npm test -- --run && cd ../web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - apps/worker/src/index.ts contains "allowHeaders" with "Authorization"
    - apps/worker/src/index.ts contains "ALLOWED_ORIGIN"
    - apps/web/src/hooks/useFetch.ts contains "VITE_WORKER_URL"
    - apps/web/src/hooks/useFetch.ts contains "Bearer"
    - `cd apps/worker && npm test -- --run` exits 0
    - `cd apps/web && npx vite build` exits 0
  </acceptance_criteria>
  <done>Worker CORS accepts requests from Pages origin, web app fetch hook sends Bearer tokens to Worker, both projects build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full auth flow end-to-end (local and production readiness)</name>
  <files>none — verification only</files>
  <action>
Human verifies the complete Phase 1 auth flow in a browser. No code changes — this is a manual verification checkpoint after Plans 01, 02, and Task 1 of this plan are complete.
  </action>
  <what-built>
Complete Phase 1 authenticated scaffold:
- Hono Worker API with Clerk JWT middleware (401/403 gates) including authorizedParties azp validation (RESEARCH Pitfall 6)
- React SPA with Clerk sign-in, org setup, and PharmIQ-branded app shell
- Worker and Web wired together with CORS and Bearer token auth
  </what-built>
  <how-to-verify>
**Pre-requisites (one-time setup — see user_setup in plan frontmatter):**
1. Create Clerk app "PharmIQ Stock Transfer" with Organizations enabled, Google + Microsoft sign-in, and `stocktransfer-worker` JWT template with claims `{ "org_id": "{{org.id}}" }`
2. Create NEON project, run `apps/worker/src/db/schema.sql` in SQL Editor, create `pharmiq_app` role with password
3. Ensure Cloudflare Workers Paid plan is active

**Local dev setup:**
1. Copy `apps/worker/.dev.vars.example` to `apps/worker/.dev.vars` and fill in real Clerk + NEON credentials
2. Copy `apps/web/.env.example` to `apps/web/.env` and fill in Clerk publishable key + Worker URL (http://localhost:8787)
3. Run `cd apps/worker && npm run dev` in one terminal
4. Run `cd apps/web && npm run dev` in another terminal

**Verification steps:**
1. Open http://localhost:5173 in browser — should redirect to /sign-in
2. Sign in with Google or email — should redirect to /org-setup (if no org) or / (if org exists)
3. If on /org-setup, create an organisation — should redirect to /
4. On /, verify: teal header with "PharmIQ" text, sidebar with greyed-out Upload/Match/Billing, placeholder content "You're all set up"
5. Open browser devtools Network tab, call the health endpoint: open console and run `fetch('http://localhost:8787/api/health', { headers: { Authorization: 'Bearer ' + (await window.Clerk.session.getToken()) }}).then(r => r.json()).then(console.log)` — should return `{ orgId: "org_xxx" }`
6. Test 401: `fetch('http://localhost:8787/api/health').then(r => console.log(r.status))` — should log `401`
7. Verify sidebar: Upload, Match, Billing items are visually disabled (greyed out, not clickable)
8. Verify Sign out button works and redirects to /sign-in

**Production readiness check (authorizedParties / azp):**
9. Confirm `apps/worker/src/middleware/auth.ts` contains `authorizedParties: [c.env.ALLOWED_ORIGIN]` — this ensures that when deployed to Cloudflare, the Worker will accept JWTs issued from the Pages domain. Without this, all production requests would return 401 due to `azp` claim mismatch (RESEARCH Pitfall 6).
10. Confirm `apps/worker/.dev.vars.example` documents that `ALLOWED_ORIGIN` is used for both CORS and azp validation, and notes the production deployment requirement.
  </how-to-verify>
  <verify>Manual — human runs 10 verification steps in browser and code review</verify>
  <done>All 10 verification steps pass: sign-in redirects, org setup works, dashboard shows branded shell, /api/health returns orgId, 401 on no token, sidebar items disabled, sign out redirects, and authorizedParties is configured for production azp validation.</done>
  <resume-signal>Type "approved" if all 10 steps pass, or describe which steps failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- Worker tests pass: `cd apps/worker && npm test -- --run`
- Web app builds: `cd apps/web && npx vite build`
- Full auth flow works end-to-end (manual verification via checkpoint)
- authorizedParties configured in auth middleware for production azp validation
- All 5 Phase 1 success criteria from ROADMAP.md are verified:
  1. User can sign in via Clerk and reach the dashboard
  2. Authenticated /api/health returns org_id from JWT
  3. Missing/invalid JWT returns 401
  4. No active org returns 403
  5. NEON tables exist with RLS (confirmed by schema.sql execution)
</verification>

<success_criteria>
- Worker and Web apps communicate via Bearer token auth
- Full sign-in -> org setup -> dashboard flow works in browser
- Auth gates (401/403) verified
- Health endpoint returns org_id from verified JWT
- PharmIQ-branded app shell visible with correct layout and copy
- authorizedParties configured for production deployment (Pitfall 6 mitigated)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

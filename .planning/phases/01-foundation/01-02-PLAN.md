---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/tsconfig.node.json
  - apps/web/vite.config.ts
  - apps/web/wrangler.jsonc
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/index.css
  - apps/web/src/pages/SignIn.tsx
  - apps/web/src/pages/Dashboard.tsx
  - apps/web/src/pages/OrgSetup.tsx
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/components/NavItem.tsx
  - apps/web/src/components/ProtectedRoute.tsx
  - apps/web/src/hooks/useFetch.ts
  - apps/web/.env.example
  - apps/web/src/vite-env.d.ts
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "User can see a sign-in page at /sign-in with Clerk authentication styled in PharmIQ teal"
    - "After sign-in, user without an org sees the org setup screen with CreateOrganization"
    - "After sign-in with an active org, user sees the app shell with teal header, sidebar with disabled nav items, and placeholder content"
    - "Unauthenticated user visiting / is redirected to /sign-in"
  artifacts:
    - path: "apps/web/src/main.tsx"
      provides: "ClerkProvider + React Router setup"
      contains: "ClerkProvider"
    - path: "apps/web/src/pages/SignIn.tsx"
      provides: "Clerk sign-in page with PharmIQ styling"
      contains: "colorPrimary"
    - path: "apps/web/src/pages/Dashboard.tsx"
      provides: "Authenticated app shell placeholder"
      contains: "You're all set up"
    - path: "apps/web/src/pages/OrgSetup.tsx"
      provides: "Org creation screen for users without an org"
      contains: "CreateOrganization"
    - path: "apps/web/src/components/AppShell.tsx"
      provides: "Header + sidebar layout wrapper"
      contains: "PharmIQ"
    - path: "apps/web/src/index.css"
      provides: "CSS custom properties for brand tokens"
      contains: "--color-teal"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "@clerk/react"
      via: "ClerkProvider with VITE_CLERK_PUBLISHABLE_KEY"
      pattern: "VITE_CLERK_PUBLISHABLE_KEY"
    - from: "apps/web/src/components/ProtectedRoute.tsx"
      to: "@clerk/react"
      via: "useAuth() hook checking isSignedIn and orgId"
      pattern: "useAuth"
    - from: "apps/web/src/hooks/useFetch.ts"
      to: "@clerk/react"
      via: "useAuth().getToken() for Bearer header"
      pattern: "getToken"
---

<objective>
React SPA with Clerk authentication, PharmIQ-branded app shell, and Cloudflare Pages config.

Purpose: Delivers the frontend half of the authenticated scaffold. Users can sign in via Clerk, create/join an org, and see the branded app shell with disabled navigation placeholders for future phases.

Output: Vite + React SPA deployable on Cloudflare Pages with sign-in, org setup, and dashboard routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@brand-identity-pharma-apps/brand-identity/brand-guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React project with Clerk, routing, and Cloudflare Pages config</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/tsconfig.app.json,
    apps/web/tsconfig.node.json,
    apps/web/vite.config.ts,
    apps/web/wrangler.jsonc,
    apps/web/index.html,
    apps/web/src/main.tsx,
    apps/web/src/App.tsx,
    apps/web/src/index.css,
    apps/web/src/vite-env.d.ts,
    apps/web/src/components/ProtectedRoute.tsx,
    apps/web/src/hooks/useFetch.ts,
    apps/web/.env.example
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
Create the `apps/web/` directory structure per D-01. Initialize with npm and install dependencies.

**package.json** — name: `pharmiq-stock-transfer-web`. Dependencies: `@clerk/react`, `react`, `react-dom`, `react-router`, `lucide-react`. Dev dependencies: `vite`, `@vitejs/plugin-react`, `@cloudflare/vite-plugin`, `tailwindcss`, `@tailwindcss/vite`, `typescript`, `@types/react`, `@types/react-dom`. Scripts: `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`.

**vite.config.ts**:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

**wrangler.jsonc** (per RESEARCH Pattern 6 — Pages asset config):
```jsonc
{
  "name": "pharmiq-stock-transfer-web",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
```

**index.html** — standard Vite React template. Include Google Fonts link tags for Space Grotesk (400, 600) and Inter (400, 600) per UI-SPEC:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600&family=Inter:wght@400;600&display=swap" rel="stylesheet">
```
Title: "PharmIQ Dead-Stock Optimizer".

**src/index.css** — Tailwind v4 import + brand CSS custom properties per UI-SPEC Color section:
```css
@import "tailwindcss";

:root {
  /* Brand */
  --color-teal:          #0F766E;
  --color-teal-light:    #14B8A6;
  --color-teal-dark:     #0D5D5A;
  --color-amber:         #D97706;
  --color-navy:          #0F172A;

  /* Surface */
  --color-surface:       #FFFFFF;
  --color-surface-gray:  #F8FAFC;
  --color-border-light:  #E2E8F0;
  --color-border-mid:    #CBD5E1;

  /* Text */
  --color-text-primary:  #0F172A;
  --color-text-secondary:#475569;
  --color-text-muted:    #94A3B8;

  /* Semantic */
  --color-success:       #10B981;
  --color-warning:       #D97706;
  --color-critical:      #EF4444;
  --color-info:          #0EA5E9;
}

body {
  font-family: 'Inter', system-ui, sans-serif;
  color: var(--color-text-primary);
  background: var(--color-surface);
}

h1, h2, h3, h4, h5, h6 {
  font-family: 'Space Grotesk', system-ui, sans-serif;
}
```

**src/vite-env.d.ts** — Vite environment type reference: `/// <reference types="vite/client" />`.

**src/main.tsx** (per RESEARCH Pattern 4, D-09):
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { ClerkProvider } from '@clerk/react';
import { BrowserRouter } from 'react-router';
import App from './App';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ClerkProvider
      publishableKey={import.meta.env.VITE_CLERK_PUBLISHABLE_KEY}
      afterSignOutUrl="/sign-in"
    >
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </ClerkProvider>
  </StrictMode>
);
```

**src/App.tsx** — Route definitions:
```typescript
import { Routes, Route, Navigate } from 'react-router';
import ProtectedRoute from './components/ProtectedRoute';
import SignIn from './pages/SignIn';
import Dashboard from './pages/Dashboard';
import OrgSetup from './pages/OrgSetup';

export default function App() {
  return (
    <Routes>
      <Route path="/sign-in" element={<SignIn />} />
      <Route path="/org-setup" element={
        <ProtectedRoute requireOrg={false}>
          <OrgSetup />
        </ProtectedRoute>
      } />
      <Route path="/" element={
        <ProtectedRoute requireOrg={true}>
          <Dashboard />
        </ProtectedRoute>
      } />
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  );
}
```

**src/components/ProtectedRoute.tsx** (per AUTH-01, AUTH-03):
- Uses `useAuth()` from `@clerk/react` to check `isSignedIn`, `isLoaded`, and `orgId`.
- If not loaded, return `null` (or a loading spinner).
- If not signed in, `<Navigate to="/sign-in" />`.
- If `requireOrg` is `true` and `orgId` is null/undefined, `<Navigate to="/org-setup" />`.
- Otherwise, render `children`.

**src/hooks/useFetch.ts** (per RESEARCH — Clerk React get JWT example):
- Uses `useAuth()` to get `getToken`.
- Exports a function that wraps `fetch`, adding `Authorization: Bearer ${token}` header.
- Base URL from `import.meta.env.VITE_WORKER_URL`.

**.env.example**:
```
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
VITE_WORKER_URL=http://localhost:8787
```

Run `cd apps/web && npm install`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contains "@clerk/react" in dependencies
    - apps/web/package.json contains "react-router" in dependencies
    - apps/web/package.json contains "lucide-react" in dependencies
    - apps/web/package.json contains "tailwindcss" in devDependencies
    - apps/web/vite.config.ts contains "@tailwindcss/vite"
    - apps/web/wrangler.jsonc contains "single-page-application"
    - apps/web/index.html contains "Space+Grotesk"
    - apps/web/index.html contains "Inter"
    - apps/web/src/index.css contains "--color-teal"
    - apps/web/src/index.css contains "--color-navy"
    - apps/web/src/index.css contains "@import \"tailwindcss\""
    - apps/web/src/main.tsx contains "ClerkProvider"
    - apps/web/src/main.tsx contains "VITE_CLERK_PUBLISHABLE_KEY"
    - apps/web/src/components/ProtectedRoute.tsx contains "useAuth"
    - apps/web/src/components/ProtectedRoute.tsx contains "Navigate"
    - apps/web/src/hooks/useFetch.ts contains "getToken"
    - apps/web/src/hooks/useFetch.ts contains "Authorization"
    - `cd apps/web && npx vite build` exits 0
  </acceptance_criteria>
  <done>Vite + React project scaffolded with Clerk auth provider, React Router routing (sign-in, org-setup, dashboard), Tailwind v4, brand CSS tokens, protected route component, and Cloudflare Pages config. Build completes successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build PharmIQ-branded app shell, sign-in page, and org setup screen</name>
  <files>
    apps/web/src/pages/SignIn.tsx,
    apps/web/src/pages/Dashboard.tsx,
    apps/web/src/pages/OrgSetup.tsx,
    apps/web/src/components/AppShell.tsx,
    apps/web/src/components/NavItem.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    apps/web/src/main.tsx,
    apps/web/src/App.tsx,
    apps/web/src/index.css,
    apps/web/src/components/ProtectedRoute.tsx
  </read_first>
  <action>
**src/pages/SignIn.tsx** (per D-09, UI-SPEC Sign-in page section):
- Layout: full-page centered on `bg-[#F8FAFC]` background.
- Centered card: `max-w-[400px]`, `p-8` (32px), `rounded-lg`, `border border-[#E2E8F0]`, `bg-white`.
- Above the Clerk component, heading: "Sign in to PharmIQ" — `<h1>` using `font-['Space_Grotesk'] font-semibold text-2xl text-[#0F172A] tracking-[-0.01em]`.
- Sub-heading: "Dead-Stock Optimizer" — `text-base text-[#475569] font-['Inter']`.
- Clerk `<SignIn />` component with `appearance` prop:
  ```typescript
  appearance={{
    variables: {
      colorPrimary: '#0F766E',
      colorText: '#0F172A',
      colorBackground: '#FFFFFF',
      fontFamily: 'Inter, system-ui, sans-serif',
    }
  }}
  ```
- `routing="path"` and `path="/sign-in"` on the `<SignIn />` component.
- `fallbackRedirectUrl="/"` to redirect to dashboard after sign-in.

**src/components/AppShell.tsx** (per D-10, UI-SPEC Header + Sidebar sections):
- Accepts `children` as prop.
- **Header**: height 56px (`h-14`), `bg-[#0F766E]`, flex row, items-center, `px-6`.
  - Left: "PharmIQ" text in `font-['Space_Grotesk'] font-semibold text-[32px] text-white leading-[1.2] tracking-[-0.01em]`.
  - Right: Clerk `<UserButton />` component (import from `@clerk/react`).
  - Skip link as first child inside header: `<a href="#main-content" className="sr-only focus:not-sr-only ...">Skip to main content</a>`.
- **Sidebar**: width 240px (`w-60`), fixed left, `bg-[#F8FAFC]`, `border-r border-[#E2E8F0]`, flex column, full height minus header.
  - Nav section: `<nav aria-label="Main navigation">` containing 3 NavItem components:
    - Upload (icon: `Upload` from lucide-react)
    - Match (icon: `GitCompare` from lucide-react)
    - Billing (icon: `CreditCard` from lucide-react)
  - All three disabled per D-11.
  - Footer section: separated by `border-t border-[#E2E8F0]`, containing:
    - "Settings" NavItem (icon: `Settings` from lucide-react) — disabled.
    - "Sign out" — Clerk `<SignOutButton>` wrapping a styled `<button>` (not disabled). Text `text-[13px] text-[#475569]`, `cursor-pointer`. Icon: `LogOut` from lucide-react.
- **Main content**: `<main id="main-content">` area to the right of sidebar, `p-8` (32px), `bg-white`, renders `{children}`.

**src/components/NavItem.tsx** (per D-11, UI-SPEC Sidebar section):
- Props: `icon: React.ReactNode`, `label: string`, `disabled: boolean`, `href?: string`.
- Renders as `<a>` tag.
- Height minimum 44px (`min-h-[44px]`), padding `py-2 px-4` (8px 16px).
- Text: `text-[13px] font-['Inter'] text-[#475569]`.
- Icon: 16px, stroke-width 1.5, same color as text. Add `aria-hidden="true"` to icon.
- Gap between icon and label: 4px (`gap-1`).
- When disabled: `aria-disabled="true"`, `pointer-events-none`, `opacity-40`, `cursor-not-allowed`.
- Disabled nav tooltip: `title="Coming soon"` on the `<a>` element.
- When active (future use): 3px teal left border + `text-[#0F766E]`.

**src/pages/Dashboard.tsx** (per D-10, UI-SPEC Main content section):
- Wrapped in `<AppShell>`.
- Placeholder heading: "You're all set up" — `<h1>` in Space Grotesk 600, 24px, `text-[#0F172A]`.
- Placeholder body: "Upload and Match are coming shortly — check back once you're ready to transfer dead stock." — `text-base text-[#94A3B8]` (muted).

**src/pages/OrgSetup.tsx** (per UI-SPEC Organisation setup screen, RESEARCH open question 1):
- Layout: full-page centered on `bg-[#F8FAFC]` background (same layout as sign-in).
- Centered card: same styling as sign-in card.
- Heading: "Set up your pharmacy group" — `<h1>` Space Grotesk 600 24px.
- Body: "You need a pharmacy group to use Dead-Stock Optimizer." — Inter 400 16px `text-[#475569]`.
- Clerk `<CreateOrganization />` component (import from `@clerk/react`).
- After org creation, Clerk redirects; ProtectedRoute will then allow access to `/`.
  </action>
  <verify>
    <automated>cd apps/web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/pages/SignIn.tsx contains "Sign in to PharmIQ"
    - apps/web/src/pages/SignIn.tsx contains "Dead-Stock Optimizer"
    - apps/web/src/pages/SignIn.tsx contains "colorPrimary: '#0F766E'"
    - apps/web/src/pages/Dashboard.tsx contains "You're all set up"
    - apps/web/src/pages/Dashboard.tsx contains "Upload and Match are coming shortly"
    - apps/web/src/pages/Dashboard.tsx contains "AppShell"
    - apps/web/src/pages/OrgSetup.tsx contains "Set up your pharmacy group"
    - apps/web/src/pages/OrgSetup.tsx contains "CreateOrganization"
    - apps/web/src/components/AppShell.tsx contains "PharmIQ"
    - apps/web/src/components/AppShell.tsx contains "UserButton"
    - apps/web/src/components/AppShell.tsx contains "Skip to main content"
    - apps/web/src/components/AppShell.tsx contains 'aria-label="Main navigation"'
    - apps/web/src/components/NavItem.tsx contains "aria-disabled"
    - apps/web/src/components/NavItem.tsx contains "Coming soon"
    - apps/web/src/components/NavItem.tsx contains "pointer-events-none"
    - apps/web/src/components/NavItem.tsx contains "opacity-40"
    - `cd apps/web && npx vite build` exits 0
  </acceptance_criteria>
  <done>PharmIQ-branded sign-in page with Clerk component, org setup screen with CreateOrganization, and app shell with teal header, disabled sidebar nav (Upload, Match, Billing), sign out button, and placeholder dashboard content. All copy matches the UI-SPEC copywriting contract. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vite build` succeeds (production build)
- Sign-in page has Clerk `<SignIn />` with `colorPrimary: '#0F766E'`
- Dashboard uses AppShell with header, sidebar, and placeholder content
- OrgSetup page renders Clerk `<CreateOrganization />`
- ProtectedRoute redirects unauthenticated users to /sign-in
- ProtectedRoute redirects users without org to /org-setup
- All nav items are disabled with `aria-disabled="true"` and `opacity: 0.4`
- CSS custom properties declare full brand token set
</verification>

<success_criteria>
- React SPA at apps/web/ builds and deploys to Cloudflare Pages
- Sign-in page with Clerk authentication at /sign-in
- Org setup screen at /org-setup for users without an org
- Authenticated app shell at / with PharmIQ branding
- Sidebar has disabled Upload, Match, Billing nav items
- All copy matches UI-SPEC copywriting contract exactly
- Brand CSS tokens (teal, amber, navy) declared as custom properties
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
